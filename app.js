function toggleMenu() {
    const menu = document.querySelector(".toggle-links");
    const icon = document.querySelector(".toggle-icon");
    menu.classList.toggle("open");
    icon.classList.toggle("open");
}
